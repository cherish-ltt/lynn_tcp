# 审查报告：DDD + 洋葱架构重构

**审查时间**：2026-05-20 23:01
**审查范围**：Git diff HEAD（29 个文件变更，4616 行删除 / 25 行新增）
**功能目标**：将扁平单体结构重构为 DDD + 洋葱架构，保持所有公共 API 签名不变。

---

## 严重问题（Critical）

### C1. `idle_count` 递增语句无效——退出空闲检测机制完全失效

**文件**：`src/infrastructure/tcp/reactor.rs` **第 155 行**
**代码**：
```rust
idle_count.saturating_add(1);  // 计算结果被丢弃！
```
**问题**：`saturating_add` 返回新值但不修改原变量。此处应为 `idle_count = idle_count.saturating_add(1)`。当前代码导致 `idle_count` 永远为 0，使得空闲等待始终走 `yield_now()` 路径，不会进入 sleep 退避。高负载场景下 CPU 占用会显著升高。

**严重等级**：**critical** — 运行时行为错误，影响性能。

**风险**：高 — 这是从旧代码 `app/tcp_reactor/reactor.rs` 迁移时引入的 bug（旧代码可能使用 `+=`）。审查旧文件确认：

<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="bash">
<｜｜DSML｜｜parameter name="command" string="true">cd /home/ghyper9023/桌面/lynn_tcp && git show HEAD:src/app/tcp_reactor/reactor.rs 2>/dev/null | grep -n "idle_count" | head -10