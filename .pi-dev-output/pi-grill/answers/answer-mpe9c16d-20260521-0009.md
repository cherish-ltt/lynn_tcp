[feat] 在 当前目录 中实现 examples文件夹非rust cargo.toml标准示例；examples较少，需补充至少3个；

**角色**：你是一个资深 rust 工程师。
**背景**：项目使用 rust ddd tcp 洋葱结构，当前缺少 examples文件夹非rust cargo.toml标准示例；examples较少，需补充至少3个；。
**任务**：
1. 先分析代码库结构，给出逐步实施计划（列出要修改/创建的文件、数据库迁移、对现有代码的假设）。
2. 计划经我确认后再编写代码。
3. 实现后编写测试用例验证核心逻辑，并运行 cargo fmt/test/check/clippy 干净 确认通过。
**输出**：提供 unified diff 和两句话的变更说明。
**约束**：禁止顺手重构无关代码；保持所有公共 API 签名兼容；不要为假设性需求添加抽象层。
**验证**：运行 cargo fmt/test/check/clippy 干净 确保无回归。