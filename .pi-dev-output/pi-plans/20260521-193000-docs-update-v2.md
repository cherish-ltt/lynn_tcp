# 文档更新 (v2.0 重构) — 实施计划

## 概述

项目已完成从 v1.x 到 v2.0 的重大重构：
- DDD + 洋葱架构分层（domain / application / infrastructure）
- 7 个可运行示例（覆盖 server、client、自定义协议、metrics）
- Cargo.toml 版本更新为 `2.0.0`，edition 更新为 `2024`，rust-version 更新为 `1.92`

当前文档现状：

| 文档 | 状态 | 问题 |
|------|------|------|
| `README.md` | ❌ 过时 | 仍引用 v1.x api（`"lynn_tcp = "1"`）；`with_server_ipv4` 示例代码与 2.0 源码实际签名不同；`Cargo.toml` 使用 git 方式而非 crates.io；缺少示例说明 |
| `README_ZH.md` | ❌ 过时 | 同上；中文版使用 git 依赖方式而未用 crates.io；api 方法名不符 |
| `version.md` | ❌ 缺少 | 没有 v2.0.0 的版本记录；v1.3.x plan 仍在文档中（实际已跳过） |
| `METRICS.md` | ⚠️ 引用过时版本 | 举例使用 `"lynn_tcp = { version = "1.3" }"` |
| `update_logs/` | ⚠️ 缺少 | 无 v2.0.0 更新日志 |
| `lib.rs` 内嵌文档 | ✅ 正确 | 与 2.0 API 保持一致（重构时同步更新了） |

本计划解决以上文档的过时问题，按照 **概述 → 快速开始 → 详细说明 → 常见问题** 结构重组 README。

## 文件清单

### 修改文件

| 文件路径 | 改动描述 | 风险等级 |
|---------|---------|---------|
| `README.md` | 全面重构为新的逻辑结构；更新版本/示例/api 引用至 v2.0；保留原有图片链接和测试数据 | 中 |
| `README_ZH.md` | 与英文版同步重构；修复 git 依赖地址问题 | 中 |
| `version.md` | 追加 v2.0.0 版本记录；更新未来规划 | 低 |
| `METRICS.md` | 更新版本引用（v1.3 → v2.0.0） | 低 |
| `Cargo.toml` | 仅检查 description、keywords 是否需要微调 | 低 |

### 新增文件

| 文件路径 | 用途说明 |
|---------|---------|
| `update_logs/v2.0.0.md` | v2.0.0 完整更新日志（DDD 重构 + 新示例 + 架构变更） |

### 删除文件

无。（`src/lib.rs.bak` 是重构前的备份，也可删除，但不属于本次文档任务范围）

## 实施步骤

### 步骤 1：更新 `version.md` — 追加 v2.0.0 版本记录

- **前置条件**：无
- **改动文件**：`version.md`
- **改动内容**：
  1. 在 `v1.3.x - plan` 之前插入 `v2.0.0 - release` 章节
  2. 保留原有 v1.x 版本历史不变
  3. 新增内容的要点：
     - 版本号: `2.0.0`
     - 架构重构为 DDD + 洋葱分层
     - 新增 7 个示例（basic_server, custom_config_server, custom_protocol, echo_server_client, multi_route_service, custom_protocol_full, metrics_example）
     - Rust edition 升级到 2024，rust-version 升级到 1.92
     - 完整的 API 向后兼容（public API 不变）
  4. 更新 "v1.3.x - plan" 部分的注释，说明 v1.3.x 系列已跳过
- **验证方式**：`cat version.md` 确认 v2.0.0 条目存在且格式一致

### 步骤 2：创建 `update_logs/v2.0.0.md` — 详细更新日志

- **前置条件**：无
- **改动文件**：`update_logs/v2.0.0.md`（新增）
- **改动内容**：
  - 遵循现有 `update_logs/v1.2.5.md`、`v1.3.0.md` 的文件格式
  - 标题：`# v2.0.0 变更日志`
  - 发布日期：2026-05-21（或当天日期）
  - 概述：从 v1.2.x 直接跳到 v2.0.0 的原因（架构重构）
  - 关键改动：
    - **架构重构**：DDD + 洋葱架构分层
      - `domain/` — 纯业务逻辑（model, routing, handler 抽象）
      - `application/` — 编排层（server, client）
      - `infrastructure/` — 具体实现（TCP reactor, metrics, validation, protocol）
      - `src/lib.rs` — 接口层（公开 API 保持不变）
    - **示例补充**：新增 7 个示例（分别列出名称和用途）
    - **Rust 版本升级**：edition 2024, rust-version 1.92
    - **依赖更新**：tokio 1.52.3, crossbeam-deque 0.8.6 等
  - 向后兼容性说明
  - 升级指南（从 v1.x 升级到 v2.0.0）
- **验证方式**：`cat update_logs/v2.0.0.md` 确认格式一致、内容完整

### 步骤 3：重构 `README.md` — 全面更新至 v2.0

- **前置条件**：步骤 1 完成（version.md 指向 v2.0.0）
- **改动文件**：`README.md`
- **改动内容**（完整重写，保留以下元素）：
  1. **徽章和语言切换** — 保留所有 GitHub badges，保留英文/简体中文切换链接
  2. **概述**（重组）
     - 保留 `Lynn_tcp is a lightweight TCP server framework`
     - 保留 5 个 Keywords（精简描述）
     - 保留 tips 说明（消息转发、长链接 TCP 游戏服务器）
     - 新增架构分层简介（一行说明 DDD 分层）
  3. **快速开始**（全新）
     - dependencies 改为 `lynn_tcp = "2"`（原来用 git 方式）
     - 分别列出 full features / server / client 三种 Cargo.toml 配置
     - **最关键修正**：示例代码中的 api 方法名必须与 2.0 源码一致
       - README 中当前用 `with_server_ipv4` → 2.0 的实际 API 是 `with_addr`
       - README_ZH 中当前用 `with_server_ipv4` → 同上
       - 需要阅读 `server_config.rs` 和 `client_config.rs` 确认 builder 方法的正确签名
  4. **示例**（新增—在 README 中嵌入真实可运行示例）
     - 添加一个 `echo_server_client` 的简化版代码示例（md 语法高亮）
     - 这个示例需要**真实可运行**，可以用 `cargo run` 验证
     - 代码示例展示：创建 server + client，发送消息，接收 echo 响应
     - 同时在 README 中列出所有 7 个示例及其 `cargo run` 命令
  5. **Features** — 保留原有 server/client 两个 feature 说明
  6. **Road maps** — 保留 ✅ 和计划功能；更新为 2.0 的规划
     - 已完成的 ✅ 保持不变
     - 计划功能：TLS1.3（计划 v2.1.0），取消旧的 v1.3.0 等引用
  7. **Flow chart** — **保留原图链接不变**（`FlowChart.png`）
  8. **Release note** — 指向 `version.md`
  9. **Test results** — **保留所有测试数据不变**（v1.1.x 和 v1.2.x 的 benchmark 数据）
  10. **License 和 Contribution** — 保留不变
  11. **新增部分**：
      - 新增 Architecture 章节，简要描述 3 层架构
      - 新增 Examples 章节，列出所有示例及用途
      - 新增 FAQ（常见问题）章节
- **验证方式**：
  - `cat README.md` 确认内容完整
  - 手动检查：示例代码中的方法名（`LynnServerConfigBuilder::with_addr` 等）与实际源码一致
  - 确认所有图片链接、徽章链接未被删除

### 步骤 4：重构 `README_ZH.md` — 与英文版同步更新

- **前置条件**：步骤 3 完成
- **改动文件**：`README_ZH.md`
- **改动内容**：
  - 与英文版保持相同的结构（概述 → 快速开始 → 示例 → 详细说明 → FAQ）
  - **修复关键问题**：中文版当前使用 git 依赖方式而非 crates.io
    - 当前：`lynn_tcp = { git = "https://github.com/cherish-ltt/lynn_tcp.git", branch = "main" }`
    - 改为：`lynn_tcp = "2"`（与英文版一致，使用 crates.io 发布版本）
  - **修复 API 方法名**：`with_server_ipv4` → `with_addr`
  - 翻译步骤 3 中新增的内容（Architecture、Examples、FAQ）
  - 保留所有 ✅ checkbox 和计划表中的中文描述
  - **保留** FlowChart 图片链接
  - **保留** 测试结果表格数据
- **验证方式**：
  - `cat README_ZH.md` 确认内容完成
  - 对比英文版结构，确认一致

### 步骤 5：更新 `METRICS.md` 版本引用

- **前置条件**：无
- **改动文件**：`METRICS.md`
- **改动内容**：
  - 将文档中所有引用 v1.3 版本号的地方更新为 v2.0.0
    - 快速开始中的版本号：`lynn_tcp = { version = "2.0.0", features = ["server", "metrics"] }`
    - Docker Compose 示例中的版本号
    - 代码示例中的版本注释
  - Metrics 功能本身未变化，不需要修改技术内容
- **验证方式**：`grep -n "1.3" METRICS.md` 确认无处遗漏

### 步骤 6：最终验证

- **前置条件**：步骤 1-5 完成
- **改动文件**：无
- **验证方式**：
  ```bash
  # 1. 确认所有文档文件存在
  ls -la README.md README_ZH.md version.md METRICS.md update_logs/v2.0.0.md

  # 2. 确认 README 中的依赖版本正确
  grep "lynn_tcp" README.md

  # 3. 确认示例代码中的 API 方法名与实际源码匹配
  grep "with_addr" README.md README_ZH.md

  # 4. 确认 version.md 包含 v2.0.0 记录
  grep "v2.0.0" version.md

  # 5. 确认图片链接保留
  grep "FlowChart" README.md

  # 6. 确认测试数据保留
  grep "Responses/Seconds" README.md
  ```

## 依赖关系

```
步骤 1 (version.md)
  └─→ 步骤 3 (README.md 将其引用为 Release note)

步骤 2 (update_logs/v2.0.0.md) — 独立，可在任意步骤完成

步骤 3 (README.md) — 核心改动
  └─→ 步骤 4 (README_ZH.md，与其结构同步)

步骤 5 (METRICS.md) — 独立

步骤 6 (最终验证)
```

## 关键验证点：示例代码中的 API 方法名

必须确认 2.0 源码中的 builder API 与 README 中的示例代码一致。以下是需要验证的方法签名：

```
LynnServerConfigBuilder
  .with_addr("0.0.0.0:9177")     → 不再是 with_server_ipv4
  .with_server_max_connections()   → 签名是否变化？
  .with_server_max_taskpool_size() → 签名是否变化？
  .with_message_header_mark()     → 新增？
  .with_message_tail_mark()       → 新增？

LynnClientConfigBuilder
  .with_server_addr()             → 不再是 with_server_ipv4
  .with_message_header_mark()     → 新增？
  .with_message_tail_mark()       → 新增？

LynnClient
  .new_with_addr()                → 不再是 new_with_ipv4
  .new_with_config()              → 保持？
```

**⚠️ 必须读取 `server_config.rs` 和 `client_config.rs` 确认实际方法签名，不要猜测。**

## 待确认事项

| 序号 | 事项 | 如何确认 |
|------|------|---------|
| 1 | `LynnServerConfigBuilder` 是否有 `with_addr` 方法？ | 阅读 `src/application/server/server_config.rs` |
| 2 | `LynnClientConfigBuilder` 是否有 `with_server_addr` 和 `with_message_header_mark`？ | 阅读 `src/application/client/client_config.rs` |
| 3 | `LynnClient` 是否有 `new_with_addr` 方法？ | 阅读 `src/application/client/lynn_client.rs` |
| 4 | 中文版 README 中依赖是否从 git 方式改为 crates.io？ | 定位到 `README_ZH.md` 的 dependencies 段落 |
| 5 | FlowChart 和测试数据是否保留？ | 在重写后的 README 中确认 |
| 6 | crates.io 上是否已发布 v2.0.0？ | 确认后可在 README 中使用 `"lynn_tcp = "2"` |

## 争议点

### 1. git 依赖 vs crates.io 依赖
- **观点 A**（推荐）：使用 `lynn_tcp = "2"`（crates.io 发布版本），安装简单，无需 git
- **观点 B**：保留 git 方式（`branch = "main"`），用户可以获取最新未发布代码
- **现状**：中文版用 git 方式，英文版用 `"1"`。建议统一为 crates.io。
- **结论**：无共识，建议保留两种方式（crates.io 为主，git 为备选注释）

### 2. README 中测试数据的保留
- **观点 A**（推荐）：保留 v1.1.x 和 v1.2.x 的测试数据，因为那是真实的 benchmark 结果
- **观点 B**：删除旧版数据，只保留最新版本
- **结论**：无共识，建议保留（数据是真实的性能参考，删除即丢失信息）
